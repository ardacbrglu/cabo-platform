name: Lint GitHub Actions
on:
  pull_request:
  push:
    branches: [ main, dev ]
permissions:
  contents: read
jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: rhysd/actionlint@v1
