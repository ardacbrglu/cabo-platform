title = "cabo-platform allowlist"
[allowlist]
  description = "known non-secrets & public envs"
  regexes = [
    '''(?i)NEXT_PUBLIC_[A-Z0-9_]+''',
    '''(?i)dummy|example|sample|test|placeholder|changeme'''
  ]
  paths = [
    '''^public/''',
    '''^package(-lock)?\.json$'''
  ]
