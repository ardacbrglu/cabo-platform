name: zap-full-scan

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Opsiyonel: Hedef URL (repo var. ZAP_TARGET yerine geçer)'
        required: false
  schedule:
    - cron: "0 2 * * 1"  # Her Pazartesi 02:00 UTC

permissions:
  contents: read   # Issue yok; sadece artifact yüklenecek

env:
  DEFAULT_TARGET: https://cabo-platform-production.up.railway.app

jobs:
  zap:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Güvenli hedef çözümleme: input > repo var > fallback
      - name: Resolve target URL (safe)
        id: resolve
        shell: bash
        env:
          INPUT_TARGET: ${{ github.event.inputs.target }}
          VAR_TARGET: ${{ vars.ZAP_TARGET }}
          DEFAULT_TARGET: ${{ env.DEFAULT_TARGET }}
        run: |
          if [ -n "$INPUT_TARGET" ]; then
            url="$INPUT_TARGET"
          elif [ -n "$VAR_TARGET" ]; then
            url="$VAR_TARGET"
          else
            url="$DEFAULT_TARGET"
          fi
          echo "Resolved target: $url"
          echo "url=$url" >> "$GITHUB_OUTPUT"

      - name: Pull ZAP image
        run: docker pull ghcr.io/zaproxy/zaproxy:stable

      - name: Run ZAP Full Scan (Docker)
        shell: bash
        run: |
          set -euo pipefail
          touch report_json.json report_md.md report_html.html
          chmod a+w report_json.json report_md.md report_html.html

          docker run --rm \
            -v "${GITHUB_WORKSPACE}:/zap/wrk/:rw" \
            --network=host \
            ghcr.io/zaproxy/zaproxy:stable \
            zap-full-scan.py \
              -t "${{ steps.resolve.outputs.url }}" \
              -a \
              -J report_json.json \
              -w report_md.md \
              -r report_html.html \
              -c .zap/rules.tsv

      - name: Upload ZAP full-scan artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-full-report
          path: |
            report_md.md
            report_html.html
            report_json.json
          retention-days: 7
